fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
qiskit>=1.0.0
qiskit-aer>=0.14.0
celery>=5.3.0
redis>=5.0.0
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0
pytest>=8.0.0
httpx>=0.27.0

# Optional provider SDKs — only needed to execute on real hardware.
# Uncomment (or install separately) the providers you intend to use.
qiskit-ibm-runtime>=0.20.0
amazon-braket-sdk>=1.70.0
azure-quantum[qiskit]>=2.0.0
azure-identity>=1.15.0
qsharp>=1.0.0
qiskit-braket-provider>=0.4.0
